[project]
name = "check-uuid-duplication"
version = "0.1.0"
description = "Checks for duplicate UUIDs in ArtiCluster files."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ruamel-yaml~=0.19"
]
