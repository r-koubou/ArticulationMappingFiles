[project]
name = "markdown-gen"
version = "1.0.0"
description = "Generate DAW articulation file lists as markdown files for Zensical from templates and data."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jinja2>=3.1.6",
]
